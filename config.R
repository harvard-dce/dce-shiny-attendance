es.host <- 'http://localhost:9200'
es.episode.index <- 'episodes.2017.05.26'
default.term <- "2017-02"
term.options <- c(
  "Spring 2017" = "2017-02",
  "Fall 2016" = "2017-01"
)

lecture.fields <- c("title", "start", "duration")
student.fields <- c("name", "huid", "status", "reg_level")
